services:
  java-maven-app-ultimate:
    image: ${IMAGE_NAME}
    ports:
      - "8080:8080"

  postgres:
    image: postgres:latest
    environment:
      POSTGRES_PASSWORD: mypassword
    ports:
      - "5432:5432"
