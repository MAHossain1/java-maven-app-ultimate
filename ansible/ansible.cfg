[defaults]
host_key_checking = False
inventory = hosts
interpreter_python = /usr/bin/python3

# enable_plugins = aws_ec2

# to use ansible playbooks with AWS EC2 instances
# specify the remote user and private key file if needed
# remote_user = ec2-user
# private_key_file = ~/.ssh/id_rsa


[privilege_escalation]
allow_world_readable_temp = True